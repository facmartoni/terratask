{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "serve": "php artisan serve",
    "share": "php artisan share",
    "start": "npm-run-all --parallel serve dev share",
    "deploy": "npm run build && git push"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.16",
    "axios": "^1.7.4",
    "concurrently": "^9.0.1",
    "laravel-vite-plugin": "^1.0",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vite": "^6.0"
  },
  "dependencies": {
    "localforage": "^1.10.0"
  }
}
